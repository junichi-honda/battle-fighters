import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.battlefighters.game',
  appName: 'バトルファイターズ',
  webDir: 'dist',
  backgroundColor: '#05060f'
};

export default config;
